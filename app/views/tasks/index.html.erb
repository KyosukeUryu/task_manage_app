<h2>タスク一覧</h2>

<table>
  <tr>
    <th>タスク名</th>
    <th>詳細</th>
  </tr>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.name %></td>
      <td><%= task.description %></td>
    </tr>
  <% end %>
</table>
