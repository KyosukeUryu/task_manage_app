<h2>管理画面</h2>
<h3>ユーザー一覧</h3>
<ul>
  <% @users.each do |user| %>
    <li>ID:<%= user.id %></li>
    <li>名前：<%= user.name %></li>
    <li>メールアドレス：<%= user.email %></li>
    <li>作成日：<%= user.created_at.strftime("%Y-%m-%d") %></li>
  <% end %>
</ul>
