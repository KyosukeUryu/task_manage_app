<h2>グループ一覧</h2>

<table>
  <tr>
    <th>グループ名</th>
    <th>作成日</th>
  </tr>
  <% @groups.each do |group| %>
    <tr>
      <td><%= group.name %></td>
      <td><%= group.created_at.strftime("%Y-%m-%d") %></td>
    </tr>
  <% end %>
</table>
